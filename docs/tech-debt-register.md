# Tech Debt Register

> Living doc. Every entry has an owner + review date. Review quarterly. Folklore dies here.
> Supersedes `tech-debt-register-inputs.md` (migrated + deleted in Phase 4).

Owners are role placeholders (`tech-lead` / `security-lead`) pending assignment. Review
dates are quarterly from creation; next sweep **2026-10-03**.

★★ **Every number below was re-measured on 2026-08-21 against `6c4e4162` (0.253.0).** The
previous revision had gone six weeks without one, and its headline figures did not survive:
TD-6's four duplication percentages and its gate, TD-7's line count, TD-1's premise, and the
whole dependency table. **Nothing in this file is gated.** Re-run the command beside a row
before acting on it; each row now carries one.

## Open items

| ID | Item | Origin | Owner | Review by | Notes |
|---|---|---|---|---|---|
| TD-1 | Next.js upgrade path unowned — and it is **not** held by anything but the lockfile | Roadmap A7 | tech-lead | 2026-10-03 | ★★★ **THE PREMISE OF THIS ROW WAS FALSE, AND CORRECTING IT MAKES THE DEBT WORSE, NOT SMALLER.** It read "Forked Next.js pinned"; neither half survives measurement. `node -e "console.log(require('./package-lock.json').packages['node_modules/next'].resolved)"` prints a **`registry.npmjs.org`** tarball — a public package, no fork — and the spec is `^16.2.11`, a **caret range**, not a pin. `AGENTS.md` says "public next" and has been right all along. What actually holds 16.2.11 is `package-lock.json` and nothing else, so `npm update next`, a lockfile conflict resolved the wrong way, or any install that does not honour the lock moves a codebase whose `AGENTS.md` opens with "This is NOT the Next.js you know" onto 16.3.2. Re-measure with `npm outdated next` — **Wanted 16.3.2 ≠ Current 16.2.11** today, which is exactly the tell the old "range-pinned, no action" framing hid. Deciding whether to pin the spec exactly, or to own the 16.3 bump, is the real work here. |
| TD-2 | Six-write-path persistence — full storage rewrite deferred | Phase 3 T10 | tech-lead | 2026-10-03 | `entity-persistence-registry.test.ts` mitigates (CSV+MD+Turso columns for `outlookEventId`); a unified codec/schema rewrite remains out of scope. ★ The mitigation is **per-slice, not general** — `AGENTS.md` records that its meta-blob rows cover different subsets of the six, and that the Turso tenant path for `activityLog` was caught by review rather than by any gate. |
| TD-3 | Auto-pull re-creates a pruned event when the entity is still pushable | v0.164 (SP5) | tech-lead | 2026-10-03 | Documented SP5 limit; a permanent per-item opt-out is future work. |
| TD-5 | `task-manager.tsx` residual size — **3020 lines**, at its baseline | Phase 3 T5 | tech-lead | 2026-10-03 | ★★ The old text said "> 800 lines", which is true and useless: it is **3020**, the largest file in the tree, and the ratchet baseline in `docs/baselines/file-sizes.json` matches it exactly — so it has **zero headroom**, and any net-positive edit fails `size:check` until something moves out. Measure with `node -e "console.log(require('fs').readFileSync('src/app/task-manager.tsx','utf8').split(String.fromCharCode(10)).length)"` — that is the gate's own metric, which is `wc -l` **+ 1**. Deliberate: orchestrator glue after 5 move-only extractions; further slicing yields pass-through modules (YAGNI). Higher-leverage move: consolidate top-level hooks/effects, not slice the render tree. |
| TD-7 | `tasks-section.tsx` residual size — **1081 lines**, at its baseline | inline-ai-edit SP1 (2026-07-03) | tech-lead | 2026-10-03 | ★★ **Recorded at 830 and now 1081 — it grew 251 lines while this row went on quoting the figure it was frozen at.** The ratchet permits that by design: it fails a file that grows past *its own baseline*, and the baseline is rewritten whenever anyone re-runs `--update`, so a fat file drifting upward is invisible here. Fat pane — owns the tasks filters/table/kanban/calendar glue. A full split (table vs board vs toolbar, gantt/reports precedent) is still deferred. Same measure command as TD-5. |
| TD-8 | Two icon packages ship side by side — the app-wide `@heroicons/react` → `lucide-react` migration is scheduled, not yet started | rich-text toolbar 0.233.0 (2026-08-11); recorded here 2026-08-21; scheduled 2026-08-21 | tech-lead | 2026-10-03 | **Status: owned, scheduled, not started.** Decided 2026-08-21: run the app-wide `@heroicons/react` → `lucide-react` migration. It is **not** in the slice that made this decision — 78 files and 70 distinct imported icon names is a slice of its own, needing its own brainstorm (`docs/work-inventory.md` §3). Re-derive the surface rather than quoting it: `grep -rln "@heroicons/react" src/app \| wc -l` (**78**), `grep -rln "lucide-react" src/app --include="*.tsx" --include="*.ts" \| wc -l` (**1**, `rich-text-toolbar.tsx`, adopted because Tiptap's own reference toolbar uses it and it ships filled/outline pairs heroicons lacks). The distinct-icon-name count needs the import-list-scoped form, not a bare `Icon`-suffix sweep: `grep -rhzoE 'import \{[^}]*\} from "@heroicons/react[^"]*"' src --include=*.tsx --include=*.ts \| tr '\0' '\n' \| grep -oE "\b[A-Za-z0-9]+Icon\b" \| sort -u \| wc -l` returns **70**; the naive form `grep -rhoE "\b[A-Z][A-Za-z0-9]+Icon\b" src \| sort -u \| wc -l` returns **102** and is wrong — it also counts local components, lucide aliases and type names. ★★ That `-h` is load-bearing and this sentence used to omit the command entirely, naming only the regex: without `-h`, grep prefixes every match with its filename, `sort -u` then counts file-and-name PAIRS rather than names, and the same sweep reports **193**. A cold reviewer built the obvious command, got 193, and reported the 102 as unreproducible — correctly, as the claim was written. Spell the command, not the pattern. ★★ **The deliberate mixed state this creates:** 78 files stay on the retired package until the migration slice runs, new code defaults to `lucide-react` effective now (`docs/CODEMAPS/dependencies.md`), and no gate enforces either side. Record that as accepted, not as a defect to "fix" by reverting the default. ★ `lucide-react` is `^1.31.0` with 1.33.0 available — a minor, still irrelevant while the blast radius is one file; do not bump it here, but it stops being irrelevant when the migration slice opens. ★ Closed as a decision at `docs/open-followups.md` §145. |

## Resolved

| ID | Item | Origin | Resolved | Notes |
|---|---|---|---|---|
| TD-6 | Duplication not yet ≤50% of the Phase 1 baseline | Phase 3/4 | **2026-08-21 — GOAL MET** | The Phase 1 baseline is `docs/baselines/jscpd-2026-07.json`, committed as "jscpd duplication baseline (3.06%)": **2701 duplicated lines / 88124 = 3.065%**. Measured today, `npm run dup:check` reports **1833 / 153046 = 1.20%** — **39% of the baseline**, against a ≤50% target. ★★ **Two things did that work and neither would have sufficed alone**: duplicated lines fell 2701→1833 (−32%) *and* the denominator grew 88k→153k. Held at the baseline's absolute 2701 lines, today's tree would read **1.77%** — over the 1.75 threshold. So do not credit this to growth dilution, and do not credit it to extraction alone. ★★★ **The theory this row ran on for 14 slices was wrong.** It tracked a "binding format", asserted "the gate is always bounded by max(tsx, ts)", and ratcheted on per-format percentages. `AGENTS.md` establishes by exit-code bisection that the gate compares **one number: the TOTAL duplicated-LINE percentage across all formats** — not per-format, and not tokens. The per-format cells the console prints are never read by the threshold, so every "binding" / "leapfrog" / "flipped" note in this row's history described an artifact of a measure nothing gates. ★ The historical progress log (14 numbered extractions, 2026-07-03 → 07-05, threshold 3.7→1.75) is in git: `git log -S"TD-6" -- docs/tech-debt-register.md`. Two durable findings survive it — intra-file **tsx self-clones** are a genuine low-risk seam (single-file, presentational, no cross-file contract), and the deferred **structural** tail (a shared scaffold for the divergent change↔raid↔stakeholder panels, Jira/Timelog API-route boilerplate, Turso schema DDL) turned out **not** to be needed to hit the target. ★ Headroom is now **0.55** (1.20 against 1.75), not the "thin 0.08" the old note claimed. Ratchet the threshold down only when a real reduction lands, and bisect by exit code — the console table flags nothing. |
| TD-4 | `sanitize*.ts` branch coverage 84.82% < the ≥90 pure-engine target | Phase 4 T2 | 2026-07-03 | `sanitize-branches.test.ts` added the validator reject/fallback/clamp arms → branch **95.70%** (lines 98.33%). Floor ratcheted to lines 95 / branches 94 in `vitest.config.ts` — re-verified 2026-08-21, still there. Not faked. |
| Playwright + axe CI-image lockstep | `@playwright/test` 1.60→1.61.1, `@axe-core/playwright` 4.11.3→4.12.1 + e2e image `v1.60.0-jammy`→`v1.61.1-jammy` | Phase 4 held | 2026-07-03 | Client + image bumped together in one MR. ★ Both have moved again since (1.62.1 / 4.13.0) — see the deferred table below. What this row establishes is the **lockstep rule**, and that still governs. |
| `@tiptap/*` set 3.26.1→3.27.1 | held: bumping only react/starter-kit skewed sub-packages → TS2769 in `rich-text-editor.tsx` | Phase 4 held | 2026-07-03 | Bumped `@tiptap/react` + `@tiptap/starter-kit`; the stale `node_modules/@tiptap` subtree pinned the transitive core/pm at 3.26.1 → ERESOLVE, cleared by removing that subtree and reinstalling so the whole tree deduped uniform. ★ The durable rule is that **the whole set moves together** — it is now seven packages deep at 3.27.1, with 3.30.2 available. |

## Deferred major dependency upgrades (Phase 4 T4 — one major per MR)

Re-measured from `npm outdated` on **2026-08-21** — 33 packages behind. The previous table was
recorded 2026-07-02 and had not been re-run since, so three of the majors below had never been
seen by it at all. Order: security → tooling → runtime. Reproduce the whole table with
`npm outdated`.

| Package | Current | Latest | Status | Notes |
|---|---|---|---|---|
| `eslint` | 9.39.4 | **10.8.1** | ⛔ BLOCKED | `eslint-config-next`'s bundled `eslint-plugin-react` calls `context.getFilename` (removed in eslint 10's flat-config API) → lint crashes. `eslint-config-next` is tied to Next.js (TD-1). **Confirmed by an executed attempt 2026-08-03** — `docs/open-followups.md` §53 carries the crash trace and the `npm view eslint-plugin-react peerDependencies` command to re-measure before retrying. ★ The target moved 10.8.0 → 10.8.1; the block is upstream and unchanged. |
| `typescript` | 6.0.3 | **7.0.2** | 🆕 NEW MAJOR — unassessed | Appeared since the last sweep. 6.0.3 landed 2026-07-03 with zero tsc errors, so this project has tracked TS majors well — but nothing has been run against 7. Assess before quoting a risk either way. |
| `jsdom` | 29.1.1 | **30.0.1** | 🆕 NEW MAJOR — unassessed | 29 landed 2026-07-03 under the note "layout-stub landmines hold". Those landmines **are** the risk surface: jsdom has no layout, and several suites here are written around exactly that. Re-verify them by name, not just the suite's exit code. |
| `@testing-library/jest-dom` | 6.9.1 | **7.0.1** | 🆕 NEW MAJOR — unassessed | Test-only, and the cheapest of the four to attempt. |
| `@types/node` | 24.13.3 | 26.2.0 | ✅ `^24` LANDED (2026-08-21) | Precondition (CI `node:24-bookworm-slim`, `engines.node >=24`) was met; measured before landing — `npx tsc --noEmit` was `EXIT=0` with **zero** new errors both before and after the bump, so no fix-up was needed. Landed `^24`, matching the CI image, **not** the newer `26.2.0` types available — going to 26 would type APIs the node 24 runtime does not have, the same over-declaration risk this row used to warn about for the untaken direction. Commit `a698eac2`. |
| `@azure/msal-browser` | 5.16.0 | 5.19.0 | ✅ major LANDED (2026-07-03) | 5.x landed green. The caveat still stands: **MSAL is mocked in unit tests**, so a live M365 token flow needs a real-tenant smoke test and there is no CI tenant. |
| `@vitejs/plugin-react` | 6.0.3 | 6.1.0 | ✅ major LANDED (2026-07-03) | Vitest transform + property suites green. |
| `@playwright/test` · `@axe-core/playwright` | 1.61.1 · 4.12.1 | 1.62.1 · 4.13.0 | ⏸ minor, **lockstep** | Not majors, but both are tied to the e2e CI image (`v1.61.1-jammy`). Bump client **and** image in one MR, or the e2e job runs a mismatched browser set — see the Resolved row. |
| `@tiptap/*` (7 packages) | 3.27.1 | 3.30.2 | ⏸ minor, **move as a set** | Mixed versions inside the set are what produced TS2769 last time. If one moves they all move; clear `node_modules/@tiptap` if npm dedupes badly. |

### Range-pinned (no action — semver range holds them)

`react` · `react-dom` (exact `19.2.4`) and `eslint-config-next` (exact `16.2.6`) — `Wanted ==
Current`, and the latest is outside the pinned spec by design.

★★★ **`next` was listed here and does not belong.** Its spec is `^16.2.11` and `npm outdated`
reports **Wanted 16.3.2**, so the range does not hold it and "no action" was never true. Moved
to **TD-1**, which is the row that has to own the decision.
