# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/test-results
/playwright-report
/playwright/.cache
/blob-report
/.playwright-mcp

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dev.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# claude
/.claude
/.reports

# skills
/.superpowers/

# local sample / pattern artifacts (not part of the repo)
/docs/patterns/

# local Turso tooling + databases (downloaded binaries / local data — not part of the repo)
/tursodb.exe
/turso_cli-installer.ps1
*.db
*.db-shm
*.db-wal
# WAL/SHM sidecars of the committed sample DB are transient — the .sqlite3 itself
# is committed (in WAL journal mode) but its sidecars must never be.
*.sqlite3-shm
*.sqlite3-wal
# Local UI-demo recorder artifacts (not part of the repo)
/demo/

# Brainstorm/plan/spec working docs + their archive — kept LOCAL only, never in the repo.
# Directory form (not `/docs/superpowers/*`) so git does not descend: nothing under here can be
# force-added back by accident. The durable cross-release register lives at docs/open-followups.md,
# which is tracked precisely because this tree is not.
/docs/superpowers/

# Agent-skill tooling artifacts (installed skills + lockfile — not part of the app)
/.agents/
/skills-lock.json

# OpenSpec working directory (not part of the app)
/openspec/

# Superpowers worktrees
/.worktrees/
